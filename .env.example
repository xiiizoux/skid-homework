# Port mapping (host:container). Compose maps ${PORT}:3000
PORT=5173

# Base runtime image for Docker builds
RUNTIME_IMAGE=alpine:3.19

# Compose project name (image name/tag prefix)
COMPOSE_PROJECT_NAME=skid-homework

# Enable Compose profiles without CLI flag (optional)
#   e.g., COMPOSE_PROFILES=dev or COMPOSE_PROFILES=prod
# If unset, pass --profile dev/--profile prod on the CLI instead.
# COMPOSE_PROFILES=
